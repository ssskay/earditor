# Extra dependencies for building the native macOS .app (on top of requirements.txt).
#   pip install -r requirements.txt -r packaging/requirements-app.txt
pywebview>=5,<7     # native window shell around the Flask review UI
py2app>=0.28,<1     # bundles everything into Earditor.app
chardet>=5,<6       # pure-Python Requests detector for frozen builds
